{
  "name": "foce-child",
  "version": "1.0.0",
  "description": "Child theme for Foce",
  "scripts": {
    "compile:scss": "sass assets/css/style.scss style.css --no-source-map --update --style expanded",
    "watch:scss": "sass assets/css/style.scss:style.css --no-source-map --watch --style expanded"
  },
  "devDependencies": {
    "sass": "^1.49.0"
  },
  "dependencies": {
    "swiper": "^10.1.0"
  }
}
